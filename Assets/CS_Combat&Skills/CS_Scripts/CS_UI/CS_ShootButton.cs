using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class CS_ShootButton : MonoBehaviour/*, IDamageable*/
{
    //This script is for testing it can be deleted in future

    public void TakeDamage(float damage, string weaponname)
    {
        /*if (transform.name.Equals("Regen"))
        {
            transform.root.GetComponent<CS_HealthAndShields>().activateRegen();

            Color colour = transform.GetChild(0).GetComponent<TextMeshProUGUI>().color;
            if (colour == Color.green)
            {
                transform.GetChild(0).GetComponent<TextMeshProUGUI>().color = Color.red;
            }
            else if (colour == Color.red)
            {
                transform.GetChild(0).GetComponent<TextMeshProUGUI>().color = Color.green;
            }   
        }
        else if (transform.name.Equals("Heal"))
        {
            transform.root.GetComponent<CS_HealthAndShields>().Heal(transform.root.GetComponent<CS_HealthAndShields>().healAmount);

        }*/
       

        
    }
}
